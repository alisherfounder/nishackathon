services:
  sqlite:
    image: coleifer/sqlite-web
    volumes:
      - sqlite_data:/data
    ports:
      - "8080:8080"
    command: ["sqlite_web", "--host", "0.0.0.0", "--port", "8080", "/data/app.db"]

volumes:
  sqlite_data:
